import { Checkout } from '@team21/ui-components';
import React, { FC } from 'react';

const CheckoutPage: FC<Record<string, never>> = () => {
    return (
        <Checkout/>
    );
};
export default CheckoutPage;
