type Mutation {
  login(
    username: String!
    password: String!
  ): LoginResponse
}

input RandInput {
  text: String!
}

type RandOutput {
  answer: String!
}

type LoginResponse {
  accessToken: String
}

