type Mutation {
  login(
    username: String!
    password: String!
  ): LoginResponse
}

type Mutation {
  registration(
    fullName: String!
    email: String!
    password: String!
  ): RegistrationResponse
}

type LoginResponse {
  accessToken: String
}

type RegistrationResponse {
  id: uuid
}

