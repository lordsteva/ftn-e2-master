CREATE TABLE "public"."wages" ("id" uuid NOT NULL, "first_name" text NOT NULL, "last_name" Text NOT NULL, "amount" numeric NOT NULL, "recipient_first_name" text NOT NULL, "recipient_last_name" text NOT NULL, "merchant_id" uuid NOT NULL, "merchant_pass" uuid NOT NULL, "user_id" uuid NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict);
