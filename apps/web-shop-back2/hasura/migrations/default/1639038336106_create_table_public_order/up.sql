CREATE TABLE "public"."order" ("id" uuid NOT NULL, "firstName" text NOT NULL, "lastName" Text NOT NULL, "country" text NOT NULL, "city" text NOT NULL, "address" text NOT NULL, "zipCode" integer NOT NULL, "phone" text NOT NULL, "userId" uuid NOT NULL, "orderSummary" jsonb NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("userId") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
